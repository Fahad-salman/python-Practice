# tuble what are tuble ?
# like list but unlike list we can not modify them , tuple as a mutably list
# tuple have two method it's count() and index() , we can use len()

my_tuple = (1,2,3,4,5,6,7)
print(my_tuple[3]) 
# we can use it like any list , we can use sort , reverse whatever 
# the idea is this value can not change .